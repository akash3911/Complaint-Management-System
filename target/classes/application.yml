spring:
  datasource:
    url: jdbc:mysql://localhost:3306/complaintdb
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

jwt:
  secret: ChangeThisSecretKeyForJwtGenerationChangeMe123456
  expiration-ms: 3600000
